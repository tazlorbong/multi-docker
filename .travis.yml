sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t tazlorbong/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true tazlorbong/react-test npm test

after_success:
  - docker build -t tazlorbong/multi-client ./client
  - docker build -t tazlorbong/multi-nginx ./nginx
  - docker build -t tazlorbong/multi-server ./server
  - docker build -t tazlorbong/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push tazlorbong/multi-client
  - docker push tazlorbong/multi-nginx
  - docker push tazlorbong/multi-server
  - docker push tazlorbong/multi-worker
